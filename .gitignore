# python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/

# node
node_modules/
.next/
# `make web-check` builds here instead of `.next` so it does not swap
# chunks out from under a running `make web`.
.next-check/
out/

# foundry
vetting/forge/out/
vetting/forge/cache/
vetting/forge/lib/

# secrets — a raw key must never reach the repo
.env
.env.*
!.env.example
*.keystore
keystore/

# local state
data/*.db
data/*.db-wal
data/*.db-shm
data/cache/

# ops
ops/KILL

.DS_Store
